export interface IUser {
      id: number;
      email: string;
      username: string;
      password: string;
      phoneNumber: number;
      address: string;
      role: number;
      status: number;
      createdAt: string;
      updatedAt: string;
}